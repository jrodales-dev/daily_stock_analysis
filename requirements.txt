# ===================================
# Sistema de Análise Inteligente de Ações Selecionadas da Bolsa A (A-Share) - Lista de Dependências
# ===================================

# Dependências Principais
python-dotenv>=1.0.0        # Gerenciamento de configuração de variáveis de ambiente
tenacity>=8.2.0             # Mecanismo de repetição (backoff exponencial)
sqlalchemy>=2.0.0           # Operações de banco de dados ORM
schedule>=1.2.0             # Agendamento de tarefas periódicas
exchange-calendars>=4.13.0   # Calendário de negociação; 4.5.x é incompatível com pandas 3 (Timedelta "T")

# Dependências de fontes de dados (estratégia multi-fontes, ordenadas por prioridade)
efinance>=0.5.5             # Priority 0: Fonte de dados Eastmoney (prioridade máxima) https://github.com/Micro-sheep/efinance
akshare>=1.12.0             # Priority 1: Fonte de dados via crawler da Eastmoney
tushare>=1.4.0              # Priority 2: API Pro do Tushare (Waditu)
pytdx>=1.72                 # Priority 2: Servidor de cotações TongDaXin
baostock>=0.8.0             # Priority 3: Dados do Baostock (Securities Treasure)
yfinance>=0.2.0             # Priority 4: Yahoo Finance (Contingência)
longbridge>=0.2.77           # Priority 5: OpenAPI da Longbridge (Contingência para ações dos EUA/Hong Kong; detecção em tempo de execução da capacidade do SDK para o caminho OAuth)
tickflow>=0.1.0            # SDK oficial do TickFlow (Issue #632, melhoria na revisão de mercado)

# Lark (Feishu)
lark-oapi>=1.0.0             # API do Lark (Feishu)

# Processamento de Dados
pandas>=2.0.0               # Análise de dados
pypinyin>=0.50.0            # Resolução de nome para código (correspondência de pinyin)
openpyxl>=3.1.0             # Parsing de Excel (.xlsx) para importação
numpy>=1.24.0               # Computação numérica
json-repair>=0.55.1         # Correção de JSON

# Análise de IA
# Mantém a versão mínima histórica, excluindo builds em quarentena e evitando quebras futuras de versões major.
litellm>=1.80.10,!=1.82.7,!=1.82.8,<2.0.0   # Cliente LLM unificado (Gemini/Anthropic/OpenAI/DeepSeek etc.)
tiktoken>=0.8.0,<0.14.0    # Tokenizador BPE para contagem de tokens de LLM
openai>=1.0.0               # SDK da OpenAI (dependência transitiva do litellm, mantida explícita)
PyYAML>=6.0                 # Parser YAML para suporte a LITELLM_CONFIG

# Motores de busca (usados para obter notícias de ações)
tavily-python>=0.3.0        # API de busca Tavily (1000 buscas gratuitas por mês)
google-search-results>=2.4.0  # SerpAPI (100 buscas gratuitas por mês)

# Requisições de Rede
requests>=2.31.0            # Requisições HTTP
markdown2>=2.4.0            # Conversão de Markdown para HTML
imgkit>=1.2.0              # Conversão de Markdown para imagem (requer instalação do wkhtmltopdf)
fake-useragent>=1.4.0       # User-Agent aleatório para evitar bloqueios
httpx[socks]                # Cliente HTTP + suporte a proxy SOCKS (dependência opcional do OpenAI)
dingtalk-stream >= 0.24.3    # SDK do DingTalk Stream
# Banco de Dados
# O SQLite é nativo do Python, não requer instalação adicional

# Bot do Discord
discord.py>=2.0.0              # Biblioteca de desenvolvimento de bots do Discord
PyNaCl>=1.5.0                  # Validação de assinatura Ed25519 para Webhook/Interação do Discord

# Extração de Conteúdo Web
newspaper3k>=0.2.8          # Extração de artigos
lxml_html_clean             # Correção para ImportError de lxml.html.clean em versões mais recentes do lxml

# Motor de templates de relatórios (Report Engine P0)
jinja2>=3.1.0               # Motor de templates Jinja2 para renderização de relatórios

# Framework Web FastAPI
fastapi>=0.109.0            # Framework Web moderno para Python
uvicorn[standard]>=0.27.0   # Servidor ASGI
python-multipart>=0.0.6     # Suporte para upload de arquivos/formulários no FastAPI

# ===================================
# Fase 1 e 2 - Nova Arquitetura API-First
# ===================================
pydantic-settings>=2.0.0
alembic>=1.13.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
celery>=5.3.6
redis>=5.0.1
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# API Clients (Fase 2)
newsapi-python>=0.2.7
newsapi-python>=0.2.7

# NLP Pipeline (Fase 2)
transformers>=4.37.0
torch>=2.2.0 --index-url https://download.pytorch.org/whl/cpu  # Versão CPU leve

# Relatórios e PDFs (Fase 4)
weasyprint>=61.0
jinja2>=3.1.3